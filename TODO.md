# TODO — future features

- [ ] Shell completion of worktree names on the command line (`ct remove <TAB>` → registered names)
- [ ] Command aliases: `remove` also accepts `delete` / `rm`, etc.

# Improvements

- [ ] Sibling links management is brittle (`symlink_siblings` / `fsops.SymlinkSiblings`): rework it

